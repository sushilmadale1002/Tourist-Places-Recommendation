services:
  - type: web
    name: travel-recommender
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers=1 --threads=1 --timeout=120 server:app
    plan: free
